# FastAPI and uvicorn are runtime dependencies of the HTTP transport.
# The core engine (boxsolver.core, boxsolver.models) needs none of them.
fastapi>=0.110
uvicorn[standard]>=0.27

# Tests and CI only.
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27          # required by fastapi.testclient
PyYAML>=6.0          # schema validation